[project]
name = "bods_api"
version = "0.1.0"
description = "Bus Open Data Service API"

[tool.setuptools.packages.find]
include = ["api*"]

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
